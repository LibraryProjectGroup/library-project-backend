version: "3.3"

services:
  efilibrary-backend-staging:
    container_name: efilibrary-backend-staging
    image: ${IMAGE_NAME}
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_SERVER: staging-mysql
      DATABASE_NAME: efilibrarydb
      DATABASE_USER: root
      DATABASE_PASSWORD: "3c35281b8a5104e58c84"
      PORT: "3000"
      OIDC_AUTH_BACKLINK_URL: "http://frontend-staging.efilibrary.jrdie.nl"
      OIDC_AUTH_REDIRECT_URL: "http://backend-staging.efilibrary.jrdie.nl"
    networks:
      - efilibrary

networks:
  efilibrary:
    external: true
