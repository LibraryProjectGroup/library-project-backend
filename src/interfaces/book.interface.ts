interface Book {
    id?: number,
    // TODO: User interface
    library_user?: string,
    title: string,
    author: string,
    topic: string,
    isbn: string,
    location: string
}

export default Book